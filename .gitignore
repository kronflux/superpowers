.worktrees/
.private-journal/
.claude/
.superpowers/
.DS_Store
node_modules/
inspo
triage/

# Eval harness lives in its own repository, cloned into evals/ for local
# development (see CLAUDE.md / README.md). It is not part of the published
# plugin, so the whole directory is ignored here.
evals/

# AI assistant artifacts
context-snapshot.json
project-map.md
session-log.md
state.md
known-issues.md

# Conductor decline markers are a local, per-developer choice — committing one
# imposes it on everyone who clones the repo.
.superpowers-no-*
